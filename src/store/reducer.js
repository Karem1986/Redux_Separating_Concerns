// src/store/reducer.js
const initialState = {
    // TODO
    hello: "world"
};

export default function reducer(state = initialState, action) {
    switch (action.type) {
        default: {
            return state;
        }
    }
}